#include <cstring>

//#include "Utilities.hpp"

void ResourceManager_Init()
{
//FbxManager *manager = FbxManager::Create();
//
//FbxIOSettings *ios = FbxIOSettings::Create( manager, IOSROOT );
//manager->SetIOSettings( ios );
//
//FbxImporter *importer = FbxImporter::Create( manager, "" );
//
//const char *filename = "assets\\models\\splash\\fmod_splash.fbx";
//
//bool success = false;
//char file_and_path[ MAX_FILEPATH_LENGTH ];
//for( uint8_t i = 0; i < cnt_of_array( RELATIVE_ROOT_DIRECTORY ) && !success; i++ )
//    {
//    sprintf_s( file_and_path, sizeof( file_and_path ), "%s%s", RELATIVE_ROOT_DIRECTORY[ i ], filename );
//    success = importer->Initialize( file_and_path, -1, manager->GetIOSettings() );
//    }
//
//auto error = importer->GetStatus().GetCode();
//if(!success )
//    {
//    printf( "Call to FbxImporter::Initialize() failed.\n") ;
//    printf( "Error returned: %s\n\n", importer->GetStatus().GetErrorString()) ;
//    }


}